[package]
name = "juq"
version = "0.1.0"
edition = "2021"

[dependencies]
candle-core = { version = "0.9", features = ["accelerate"] }
candle-nn = "0.9"
tokenizers = "0.22"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"

[profile.release]
opt-level = 3
lto = true
